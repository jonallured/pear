export class Author {
  email: string
  name: string
  username: string

  constructor(email: string, name: string, username: string) {
    this.email = email
    this.name = name
    this.username = username
  }
}
